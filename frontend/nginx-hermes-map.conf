# WebSocket connection upgrade map for Hermes runtime proxy.
# Must be at http{} level — included via conf.d alongside default.conf.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
